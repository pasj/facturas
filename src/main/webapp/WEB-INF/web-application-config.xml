<?xml  version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xmlns:p="http://www.springframework.org/schema/p" xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
		http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx.xsd">

    <bean id="propertyConfigurer" class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer"
          p:location="classpath:jdbc.properties"/>


    <!-- needed is for @Configurable -->
    <context:annotation-config />
    <context:component-scan base-package="factura" />

    <tx:annotation-driven />

    <!--========== Spring Data Source ========== -->
    <bean id="dataSource" class="org.springframework.jndi.JndiObjectFactoryBean">
        <property name="jndiName">
            <value>${jdbc.jndiName}</value>
        </property>
    </bean>

    <!--========== Hibernate SessionFactory ========== -->
    <bean id="sessionFactory"
          class="org.springframework.orm.hibernate4.LocalSessionFactoryBean">
        <property name="dataSource">
            <ref bean="dataSource" />
        </property>
        <property name="packagesToScan"
                  value="factura.modelo.Entidad" />
        <property name="hibernateProperties">
            <props>
                <prop key="hibernate.dialect">${jdbc.dialect}</prop>
                <prop key="hibernate.show_sql">${jdbc.show_sql}</prop>
                <!-- JDBC connection pool (use the built-in) -->
                <prop key="connection.pool_size">10</prop>
                <prop key="hibernate.connection.url">jdbc:postgresql://localhost:5432/facturacion</prop>
                <prop key="hibernate.connection.driver_class">org.postgresql.Driver</prop>
                <!-- Disable the second-level cache -->
                <!--<prop key="cache.provider_class">org.hibernate.cache.</prop> -->
            </props>
        </property>
        <property name="annotatedClasses">
            <list>
                <value>modelo.Entidad.Municipio</value>
            </list>
        </property>
        <property name="mappingLocations">
            <list>
                <value>classpath:modelo/Entidad/Municipio.hbm.xml</value>
                <value>classpath:factura/modelo/Entidad/Municipio.hbm.xml</value>
            </list>
        </property>
    </bean>

    <!--========== TransactionManager ========== -->
    <bean id="transactionManager"
          class="org.springframework.orm.hibernate4.HibernateTransactionManager">
        <property name="sessionFactory" ref="sessionFactory" />
    </bean>

    <!--========== Spring CustomScopeConfigurer ========== -->
    <bean class="org.springframework.beans.factory.config.CustomScopeConfigurer">
        <property name="scopes">
            <props>
                <prop key="view">factura.utils.ViewScope</prop>
            </props>
        </property>
    </bean>



    <!--========== XMLConverter ========== -->


</beans>